mcp_port: 15720

plugins:
  asr:
    enabled: false

  tts:
    enabled: true
    model_dir: /models/vits2-mix
    trt_dir: /models/vits2-mix/trt_mel20full_d50
    model_type: "mel20full_d50"
    speaker_id: 0
    speed: 1.0

  htmsg:
    enabled: false

  vop:
    enabled: false
